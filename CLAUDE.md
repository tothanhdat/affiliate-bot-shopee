# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Bot nhận link sản phẩm (Shopee, Lazada) qua Telegram và Zalo (tự động hoá 1 tài khoản Zalo cá nhân qua thư viện không chính thức `zca-js` — **không phải Zalo OA**, quyết định đã chốt, đừng đề xuất quay lại Zalo OA trừ khi user chủ động nhắc lại), tự động nhận diện merchant theo domain trong link, gắn affiliate ID, trả về short link. Hoa hồng phát sinh **được chia sẻ 10% chủ bot / 90% user** (mô hình cashback, quyết định 2026-08-17 đảo ngược ý định ban đầu là giữ 100% hoa hồng; tỷ lệ đổi từ 20/80 sang 10/90 ngày 2026-08-19, `COMMISSION_USER_SHARE_PERCENT` trong `.env` — từ 2026-08-21 `.env` chỉ còn là giá trị khởi tạo/fallback, admin đổi được ngay tại runtime qua `/admin/settings` không cần restart, xem mục `settings` trong `ledgerStore.ts` bên dưới; **đổi qua đây cũng kéo theo cảnh báo retroactive cho đơn `pending`**, xem `helpText` của `userSharePercent` trong `src/config/settingsRegistry.ts`) — **file này (`CLAUDE.md`) là nguồn duy nhất cho chi tiết implementation/kiến trúc/quyết định kỹ thuật**. Quy trình vận hành hàng ngày (đối soát, ghi nhận đơn, xử lý rút tiền — cái gì tự động/cái gì thủ công) xem `huong-dan-van-hanh-admin.md`; trải nghiệm/hướng dẫn phía user xem `huong-dan-nguoi-dung.md`; rủi ro kỹ thuật đã biết xem `rui-ro-can-giai-quyet.md` (2026-08-20: đã gộp `spec_bot_ap_ma_shopee.md`/`quy-trinh-van-hanh-cashback.md`/`phan-hoi-cai-thien-trai-nghiem-nguoi-dung.md` thành 2 file hướng dẫn trên rồi xoá 3 file cũ — đừng tìm 3 file này nữa, nội dung còn hiệu lực đã chuyển hết).

**Nguồn affiliate Shopee — ĐÃ CHỐT (cập nhật 2026-08-19), KHÔNG còn "chưa chốt" nữa**: Shopee từng từ chối cấp Open API (`app_id`/`secret_key`) cho tài khoản KOC cá nhân (2026-08-17, kết luận cuối, đừng hỏi lại Shopee về việc này) — nhưng sau đó tìm được + verify xong hướng KHÔNG cần Open API: cơ chế `an_redir` chính thức của Shopee, chỉ cần `affiliate_id` cố định của tài khoản (không phải bí mật, lấy tại `affiliate.shopee.vn/account_setting`, không cần xin cấp riêng). **Đã implement**: `ShopeeAffiliateProvider` (`src/core/providers/shopeeAffiliateProvider.ts`, T3.1) tự build `https://s.shopee.vn/an_redir?origin_link=...&affiliate_id=...&sub_id=...` — xem chi tiết trong skill `tich-hop-affiliate-provider`. `AFFILIATE_PROVIDER=shopee_direct` (mới, thứ 3 sau `mock`/`accesstrade`) dùng provider này cho Shopee, còn Lazada/TikTok Shop vẫn qua `AccesstradeProvider` như cũ (`CompositeAffiliateProvider` định tuyến theo merchant, xem `src/core/providers/index.ts`). `AccesstradeProvider` vẫn giữ nguyên, không xoá — vẫn là provider chính cho Lazada/TikTok Shop.

**Mã giảm giá chung (`getPromotions`/`formatPromotionsReply`) mặc định TẮT** (`PROMOTIONS_DISPLAY_LIMIT=0`, quyết định 2026-08-17) — trọng tâm sản phẩm giờ là cashback, không phải tra mã giảm giá. Code giữ nguyên, bật lại được qua `.env` nếu cần, không phải xoá.

**Merchant được chọn theo domain trong link, không theo room/group Telegram** — quyết định đã chốt (xem lịch sử trao đổi): đơn giản hơn room-based (không cần bảng mapping room↔merchant). Đừng tự ý đổi sang room-based routing trừ khi user yêu cầu lại.

**Zalo Group Adapter dùng API không chính thức, có rủi ro khoá tài khoản** — quyết định đã được user chấp nhận (dùng tài khoản phụ/throwaway, không dùng tài khoản chính). Đừng "sửa" sang Zalo OA chính thức để "an toàn hơn" — đó là hướng đã bị từ chối.

## Architecture

Thiết kế **core logic dùng chung + adapter mỏng theo platform**, để mở rộng sang platform chat khác hoặc merchant khác không phải viết lại logic nghiệp vụ.

```
src/core/                    Core Service — khong biet gi ve Telegram/HTTP
  merchants.ts                registry nhan dien merchant theo domain (Shopee, Lazada, TikTok Shop) — THEM MERCHANT MOI TAI DAY
  linkValidator.ts            validate URL, nhan dien merchant, tach shop_id/item_id (Shopee) hoac product_id (TikTok Shop - BAT BUOC, khong phai optional, xem NotAProductLinkError), resolve short-link redirect. Rieng Shopee: subdomain sv.shopee.vn (Shopee Video, dang link /share-video/...) bi tu choi ngay bang NotAProductLinkError qua buildParsedLink() (2026-08-28, xem chi tiet trong skill `tich-hop-affiliate-provider`) - khac cac trang Shopee khac (shop/category/campaign) van hop le du id rong.
  affiliateProvider.ts        interface AffiliateProvider (createAffiliateLink + getPromotions, ca 2 deu nhan merchant; createAffiliateLink co them shopId/itemId optional cho case nhu Shopee/TikTok Shop) — diem noi de doi/them nguon affiliate khac Accesstrade
  providers/
    accesstradeProvider.ts    tich hop that voi Accesstrade API, config campaign_id/promotions-merchant RIENG TUNG MERCHANT — xem skill tich-hop-affiliate-provider
    shopeeAffiliateProvider.ts  Shopee qua co che an_redir truc tiep (khong qua Accesstrade, khong can Open API), T3.1 — xem skill tich-hop-affiliate-provider
    compositeProvider.ts      dinh tuyen 1 AffiliateProvider khac nhau THEO TUNG MERCHANT (vd Shopee -> shopeeAffiliateProvider, Lazada/TikTok Shop -> accesstradeProvider) - dung khi AFFILIATE_PROVIDER=shopee_direct
    mockProvider.ts           gia lap, dung khi AFFILIATE_PROVIDER=mock (mac dinh, khong can credentials), co du lieu mau rieng tung merchant
    index.ts                  factory chon provider theo env AFFILIATE_PROVIDER ("mock" | "accesstrade" | "shopee_direct")
  linkResolverService.ts      dieu phoi: rate limit -> parseProductLink (xac dinh merchant) -> goi provider theo merchant -> log (kem merchant) -> tra ket qua/loi
  rateLimiter.ts               sliding-window trong bo nho, key theo "platform:userId" — cung dung lam adminLoginRateLimiter (key theo req.ip) de chan brute-force /admin/login.
  logStore.ts                  SQLite qua node:sqlite (built-in Node 22+, KHONG dung better-sqlite3 hay lib ngoai). Co cot merchant + tu migrate (ALTER TABLE) cho DB tao truoc khi co field nay. findBySubId() dung boi ledgerAdmin.ts. Co them bang short_links (T3.2, code -> target_url, dung boi route GET /s/:code).
  ledgerStore.ts                SQLite rieng (DATABASE khac logStore, xem env.ledgerDatabasePath) cho ledger chia se hoa hong (T2.1-T2.4): commission_entries (co productName tuy chon), withdrawal_requests (co bank_name/bank_account_number/bank_account_holder BAT BUOC tu 2026-08-20, xem MissingBankInfoError), dashboard_tokens, accesstrade_payments, welcome_messages (2026-08-20, dung boi tryClaimWelcomeMessage() — DM chao mung Zalo gui 1 lan/user LUC gui link san pham DAU TIEN, xem zalo/bot.ts), group_join_messages (2026-09-07, bang RIENG - dung boi tryClaimGroupJoinMessage() — DM chao mung Zalo gui 1 lan/user NGAY LUC vua duoc ADD vao group, khac trigger voi welcome_messages nen tach bang rieng thay vi them cot phan loai, tranh doi PRIMARY KEY cua bang welcome_messages da co du lieu that), settings (2026-08-21, key-value luu 6 gia tri admin chinh duoc qua /admin/settings: % hoa hong user, nguong rut tien, va 4 template tin nhan bot (them groupJoinWelcomeTemplate 2026-09-07) — key hang so xem src/core/settingsKeys.ts, danh sach field/default/helpText dung cho ca render form lan validate xem SETTINGS_REGISTRY trong src/config/settingsRegistry.ts). LedgerStore co cac getter typed doc gia tri settings hien hanh, fallback ve default do caller truyen vao neu chua co override (vd getUserSharePercent(defaultValue), getWithdrawalThresholdVnd(defaultValue) — xem cach dung trong src/index.ts va src/api/server.ts). requestWithdrawal() dong bo trong 1 method (khong await xen giua) de chong rut trung, nhan them tham so bankInfo bat buoc. Them cot moi luon phai them migration (xem migrateAddBankInfoColumns) - DB that tren may da tung tao truoc khi co cot moi se loi "no such column" neu quen. Con co listUsers()/listCommissionEntries()/getEntryById() dung rieng cho trang admin web. import_history (2026-08-23, bang moi khong can migration vi la bang moi hoan toan): recordImportHistory()/listImportHistory(limit) — lich su cac lan "ghi nhan don hang" tren `/admin/record-orders`, xem chi tiet trong shopeeReportImport.ts va adminHtml.ts.
  orderIngest.ts                Logic ghi nhan don hang DUNG CHUNG boi ledgerAdmin.ts (CLI) va route web /admin/record-orders (tach ra day de 2 noi khong lap code). recordOrderFromAccesstrade (1 don) / recordOrdersFromCsv (hang loat, 1 dong loi khong hong ca batch) deu tra ve platform/userId/userShareAmount tren dong OK. summarizeOrderResultsByUser() gom nhom theo (platform,userId) — dung de gui 1 tin thong bao gop cho moi user thay vi N tin cho N don (2026-08-20, xem "Thong bao tu dong khi don duoc xac nhan" trong spec).
  adminAuth.ts                  AdminSessionStore: session admin luu trong bo nho (khong ky/hash, mat khi restart), 1 mat khau co dinh ADMIN_PASSWORD trong .env, khong ho tro nhieu tai khoan.
  vietnamBanks.ts                VIETNAM_BANKS: danh sach ten ngan hang/vi dien tu VN pho bien (khong xac thuc gia tri, chi goi y cho <select> tren form rut tien) — xem huong-dan-nguoi-dung.md muc 7.
  errors.ts                    AppError va cac subclass — moi loi co userMessage tieng Viet an toan de hien thi truc tiep cho user
  accesstradeSync.ts             T2.1 tu dong hoa (2026-08-20, DONE cho TikTok Shop/Lazada — Shopee VAN THU CONG, xem skill `doi-soat-hoa-hong`). syncAccesstradeTransactions() goi GET /v1/transactions, tai su dung recordOrderFromAccesstrade/reverseCommissionEntry co san — khong them logic ghi ledger moi.
  shopeeReportImport.ts          (2026-08-23) importShopeeReport() — import THANG file bao cao goc Shopee Affiliate (vd AffiliateCommissionReport_*.csv tu affiliate.shopee.vn/report/conversion_report), khong can doi ten cot. Doc theo dung ten cot tieng Viet goc cua Shopee (qua parseCsv() da co san). subId tai tao tu ghep Sub_id1..Sub_id5 (noi bang "-", bo o rong) — khop dinh dang platform-userId-timestamp36-random bot tu sinh. Trang thai ghi nhan lay tu cot "Trang thai san pham lien ket" (KHONG phai "Trang thai dat hang" cap don): "Hoan thanh"->confirmed, "Dang cho xu ly" VA "Chua thanh toan" (2026-09-10: don COD user dat nhung chua tra tien - don co that nhung chua chac chan, giong het "Dang cho xu ly")->pending, "Khong hop le" VA "Da huy" (2026-09-10: user tu huy don)->reversed (CHI huy duoc entry dang pending, giong dung rule accesstradeSync.ts), gia tri la khac -> skip CA DON + canh bao (khong doan mo). So khop ten trang thai luon di qua normalize("NFC") va chap nhan ca 2 cach viet tieng Viet "hủy"/"huỷ" - danh sach gia tri da biet gom trong KNOWN_STATUS_LABELS (giu dong bo voi classifyRowStatus khi them gia tri moi). **Quet 14 bao cao that (2026-09-10) chi thay dung 4 gia tri: "Hoan thanh" (44 dong), "Dang cho xu ly" (23), "Chua thanh toan" (1), "Da huy" (1)** - rieng "Khong hop le" chua tung xuat hien lan nao trong du lieu that (giu lai vi da co san). Vong doi that da quan sat duoc: "Chua thanh toan" -> "Da huy" (don 260908GH4GRXP4, bao cao 09/09 -> 10/09). Logic nhanh trang thai mo phong y het accesstradeSync.ts (tai dung recordConversion/confirmPendingEntry/updatePendingEntry/reverseCommissionEntry/getEntryByOrderId co san). Group theo "ID don hang", cac dong cung 1 ma don duoc GOP thanh 1 entry qua mergeOrderRows() (2026-09-10, truoc do bi skip qua skippedMultiItem - counter do da bi XOA, thay bang mergedMultiItem dem so don duoc gop): cong don "Gia tri don hang (d)" + "Tong hoa hong san pham(d)" cua tung dong (**KHONG doc cot cap don "Tong hoa hong don hang(d)" - Shopee chi dien cot do o DONG DAU cua nhom, cac dong sau bo trong**); dong bi huy bi loai khoi tong (tra hang 1 phan); ca don la pending neu con BAT KY dong nao "Dang cho xu ly" (nguyen tac "chua chac chan het thi van pending", giong groupTransactionsByOrderId), reversed neu TAT CA dong deu huy; BAT KY dong nao trang thai la -> bo qua CA DON (khong ghi mot phan); dong co Sub_id rong/khac Sub_id cua don bi loai khoi tong kem canh bao; ten san pham lay tu dong hoa hong cao nhat + hau to "(+N san pham khac)". Live-verify 2026-09-10 tren bao cao that (don 260909K72F4DAY: 1 san pham chinh 306.540d/22.990,5d + 2 dong qua tang 0d) ra dung con so Shopee hien o cot "Hoa hong don hang". Luu y van dung tu 2026-08-23: Shopee tach checkout nhieu shop thanh NHIEU "ID don hang" RIENG BIET (moi shop 1 ma) - don nhieu dong chi xay ra khi mua nhieu san pham/qua tang trong CUNG 1 shop. **Phat hien quan trong tu test that**: san pham mua kem trong cung phien click (khac shop/san pham, KHONG qua link nao cua bot) van duoc Shopee tinh hoa hong cho tai khoan affiliate, nhung dong do co Sub_id1-5 RONG HOAN TOAN (khac dong co click qua link, du co the cung "Checkout id" ke can) - day la hanh vi "cua so theo doi" tieu chuan cua affiliate. Quyet dinh CHOT voi user (2026-08-23): KHONG doan gan cho user nao, dong nay bi skippedNoSubId, tien hoa hong nay mac nhien la loi nhuan rieng cua chu bot (khong chia 90/10) - co chu dich, khong phai thieu sot. Dung boi ledgerAdmin.ts (record-shopee-report) va server.ts (POST /admin/record-orders/shopee-report). Tra ve them `newOrderIds`/`statusTransitions` (2026-08-23, cho tinh nang lich su - xem LedgerStore.recordImportHistory() o tren): newOrderIds la ma don GHI MOI (confirmed hoac pending), statusTransitions CHI ghi khi trang thai THAT SU doi (vd pending->confirmed qua confirmPendingEntry, pending->reversed qua reverseCommissionEntry) - KHONG gom truong hop updatePendingEntry (van la "pending", chi refresh so lieu, khong phai transition).

src/api/server.ts             Express: POST /api/v1/resolve, GET /api/v1/logs (loc duoc theo platform va merchant), GET /health, GET /d/:token + POST /d/:token/withdraw (dashboard ca nhan + rut tien kem form ngan hang, T2.3-T2.4), GET /s/:code (redirect 302 THAT, T3.2), va toan bo /admin/* (dang nhap 1 mat khau ADMIN_PASSWORD qua AdminSessionStore, xem /admin/withdrawals, /admin/users, /admin/orders, /admin/record-orders (them POST /admin/record-orders/shopee-report 2026-08-23, xem shopeeReportImport.ts), /admin/accesstrade-payments, /admin/settings (2026-08-21, GET render form + POST validate/luu qua SETTINGS_REGISTRY, xem settingsRegistry.ts) — HTML viet tay trong adminHtml.ts). createServer() nhan them notifyUser(platform,userId,message) (goi tu index.ts, dinh tuyen Telegram/Zalo) va dashboardBaseUrl de gui thong bao don moi tu /admin/record-orders/*. Tu 2026-08-31: notifyUser cung duoc goi (best-effort, khong lam fail response) tai POST /d/:token/withdraw (bao user "da ghi nhan yeu cau rut" ngay sau requestWithdrawal thanh cong, dung formatWithdrawalRequestedReply) va POST /admin/withdrawals/:id/mark-paid (bao user "tien da chuyen" kem link dashboard sau markWithdrawalPaid, dung formatWithdrawalPaidReply) — truoc do 2 su kien nay chi bao admin (notifyAdmin) hoac khong bao ai, xem replyText.ts.
src/api/dashboardHtml.ts      Render HTML tay (khong templating lib, giong triet ly cua replyText.ts) cho trang dashboard ca nhan. Form rut tien (2026-08-20) bat buoc 3 truong ngan hang (chon tu VIETNAM_BANKS).
src/api/adminHtml.ts          Render HTML tay cho toan bo trang /admin/* (sidebar toi + topbar + card sang, khac theme tim/toi cua dashboardHtml.ts). /admin/withdrawals hien them 3 cot ngan hang user da dien (2026-08-20). /admin/orders: cot "San pham" tu dong cat bot (class .cell-truncate, kem title de xem day du khi hover) tu 2026-08-23 - tranh ten san pham dai day cot "Trang thai" ra ngoai vung nhin thay (bang dung white-space:nowrap toan cuc). renderRecordOrdersPage() (2026-08-23, sua lai 2 lan trong ngay) gio nhan (history, singleResult?, shopeeReportResult?, shopeeReportError?) - da XOA han card "Import file CSV (nhieu don cung luc)" cu (khong dung nua theo yeu cau user, route /admin/record-orders/csv da go khoi server.ts, CLI ledgerAdmin.ts record-conversions-csv van GIU NGUYEN cho TikTok Shop/Lazada). Con lai 3 card: "Ghi 1 don le", "Import bao cao goc Shopee Affiliate" (form upload + hien thi ShopeeReportImportResult qua class .summary-list), va "Lich su ghi nhan don hang" (bang moi, doc tu ledgerStore.listImportHistory(20) - moi dong la 1 lan hanh dong, cot "Loai" phan biet "csv" (import bao cao Shopee) vs "single" (form Ghi 1 don le) qua ACTION_TYPE_LABELS, cot "Don moi"/"Don doi trang thai" dien "0" neu rong). Ca route /admin/record-orders/single VA /admin/record-orders/shopee-report (server.ts) deu goi ledgerStore.recordImportHistory() sau khi xu ly xong (single luon actionType="single" + newOrderIds=[1 ma don], khong bao gio co statusTransitions; shopee-report luon actionType="csv", ghi ca khi ket qua toan so 0). PHAM VI CO CHU DICH: lich su nay CHI ghi cho 2 route WEB tren, KHONG ghi cho CLI (record-conversion/record-shopee-report chay tay) - neu sau nay can ca CLI thi phai tu them goi recordImportHistory() trong ledgerAdmin.ts.
src/api/htmlHelpers.ts        formatVnd/formatDateTime/escapeHtml dung chung boi ca dashboardHtml.ts lan adminHtml.ts. successToast(message) (2026-08-23): toast thanh cong goc tren-phai, tu bien mat sau ~4s bang THUAN CSS animation (@keyframes toast-fade dinh nghia trong <style> cua adminHtml.ts) - KHONG dung JS/setTimeout, giu dung triet ly toi thieu JS phia client. Dung cho action "import" (hien chi /admin/record-orders/shopee-report, xem adminHtml.ts) - khac han banner tinh <div class="success"> co san cho form "Ghi 1 don le" (van giu nguyen, khong doi).
src/scripts/ledgerAdmin.ts    Script chay tay (npx tsx ...) ghi nhan don hang/hoa hong thu cong. Tu 2026-08-20: KHONG con la "giai phap tam" cho TikTok Shop/Lazada nua (2 merchant nay da co accesstradeSync.ts tu dong hang ngay) - van la cong cu CHINH THUC va VINH VIEN cho Shopee (merchant nay khong bao gio tu dong hoa duoc, xem accesstradeSync.ts), va van dung duoc de ghi tay/sua tay TikTok Shop/Lazada khi can (vd don loi, can ghi truoc lich chay). doc comment dau file truoc khi dung. record-conversions-csv (2026-08-18): nhap hang loat tu file CSV (quy trinh doi soat Shopee, xem huong-dan-van-hanh-admin.md muc 3b — doi tu lich thu 5 hang tuan sang hang ngay 9h30 sang tu 2026-08-23, vi Shopee cap nhat du lieu don hang luc 9h nen can chua do tre), khong crash ca batch neu 1 dong loi (in ket qua tung dong + tong ket). File mau: src/scripts/templates/weekly-conversions.example.csv. record-shopee-report (2026-08-23, xem shopeeReportImport.ts): import THANG file bao cao goc Shopee (khong doi ten cot), day la cong cu CHINH cho quy trinh doi soat hang ngay 9h30 sang (huong-dan-van-hanh-admin.md muc 3b). Tu 2026-08-20: sau khi ghi xong tu dong goi Telegram Bot API (fetch, KHONG can bot dang chay) bao user co don moi — CHI Telegram, Zalo tu CLI bi bo qua co chu dich (rui ro DuplicateConnection voi bot that dang dang nhap tren server), dung web /admin/record-orders neu can bao ca user Zalo.
src/scripts/csv.ts            Parser CSV toi gian (khong dung lib ngoai), rieng cho record-conversions-csv
src/adapters/
  shared/replyText.ts          template tin nhan DUNG CHUNG giua Telegram va Zalo (USAGE_TEXT, formatSuccessReply/Error/Skipped/PromotionsReply, formatDashboardLinkReply, formatOrdersConfirmedReply, formatWelcomeReply, formatGroupJoinWelcomeReply) — sua text/nhan hien thi tai day, khong sua rieng tung adapter. formatSuccessReply KHONG hua so tien/% hoa hong cu the (xem T2.1-T2.4). formatOrdersConfirmedReply (2026-08-20) dung boi ca ledgerAdmin.ts lan server.ts (/admin/record-orders/*) de bao user don moi duoc xac nhan. formatWelcomeReply (2026-09-07: them tham so dashboardUrl, hien THANG link dashboard thay vi chi huong dan nhan "xemhh") dung khi user gui link san pham DAU TIEN; formatGroupJoinWelcomeReply (moi 2026-09-07, khong co placeholder dong) dung NGAY LUC user vua duoc ADD vao group — 2 template khac nhau cho 2 trigger khac nhau, xem zalo/bot.ts.
  telegram/bot.ts               Telegraf, long polling (khong dung webhook). 1 tin nhan co the chua link nhieu merchant khac nhau — moi merchant duoc reply rieng. Lenh "xemhh" (DM only) tra ve link dashboard. Khuyen mai (getPromotions) mac dinh TAT (PROMOTIONS_DISPLAY_LIMIT=0), xem code van con neu can bat lai.
  zalo/
    bot.ts                      dieu khien 1 TAI KHOAN ZALO CA NHAN qua thu vien khong chinh thuc `zca-js` (mo phong Zalo Web) — KHONG phai Zalo OA. Trong GROUP: xu ly link giong het Telegram adapter (dung chung replyText.ts). Trong DM: lenh "xemhh" tra link dashboard, moi noi dung khac IM LANG hoan toan, khong tra loi gi ca (quyet dinh goc 2026-08-17; 2026-08-20 tung doi sang tra loi 1 cau huong dan co dinh de tranh nguoi dung lan dau tuong bot loi, nhung 2026-08-21 doi lai ve im lang hoan toan theo yeu cau truc tiep cua user — ZALO_DM_HELP_TEXT da bi xoa khoi code, dung tim lai). sendDirectMessage(userId, message) (2026-08-20): gui DM chu dong toi 1 userId bat ky (khong phai tra loi message nhan duoc), dung boi notifyUser trong index.ts. Group cung tu dong DM chao mung (formatWelcomeReply) toi user vua gui link san pham DAU TIEN (2026-08-20, yeu cau truc tiep cua user) — LedgerStore.tryClaimWelcomeMessage() (bang welcome_messages moi) dam bao CHI gui 1 lan duy nhat/user, best-effort (loi gui khong chan xu ly link). formatWelcomeReply tu 2026-09-07 hien THANG link dashboard (tao qua findOrCreateDashboardToken) thay vi chi huong dan nhan "xemhh" (van con nhung chi con la cach lay LAI link neu lo mat) - CUNG NGAY sau do rut gon lai lan 2 (yeu cau truc tiep cua user, thay 2 DM chao lien tiep qua dai dong): bo doan "cach dung"/luu y Shopee/link So tay (da co o GROUP_JOIN_WELCOME_TEMPLATE_DEFAULT ben duoi, DM chao truoc do), CHI con tap trung % hoa hong + dashboard link, xung "em" dong bo cac template khac. **CHI lam cho Zalo, KHONG lam Telegram** — Telegram Bot API chan bot chu dong DM user chua tung tu nhan tin cho bot truoc ("Forbidden: bot can't initiate conversation"), dung target cua tinh nang nay (user MOI, rat co the chua tung DM bot) se loi ngay nen quyet dinh khong lam o Telegram thay vi gui 1 tinh nang loi thau am tham.
    Ngoai ra, tu 2026-09-07 (yeu cau truc tiep cua user - user moi vao group truoc do khong biet cach dung, phai nhan tin hoi rieng): bot con lang nghe event `group_event` cua zca-js, loai `GroupEventType.JOIN`, va DM chao mung NGAY LUC co thanh vien moi duoc ADD vao group (formatGroupJoinWelcomeReply, tone ngan gon/GenZ, kem link So tay hoan tien, KHONG kem dashboard) - ghi nhan DU actor la CHINH bot hay NGUOI KHAC trong group add vao (yeu cau truc tiep cua user), CHI bo qua dung 1 truong hop: phan tu trong `updateMembers` trung uid cua CHINH bot (qua `api.getOwnId()`) - do la luc bot tu duoc them vao 1 group khac, khong phai khach moi. **Bat buoc `new Zalo({ selfListen: true })` luc khoi tao** (KHONG phai qua `login()`) - mac dinh thu vien tu nuot am tham moi `group_event` co `event.isSelf === true`, ma co nay dung true CA KHI chinh tai khoan bot la actor thuc hien hanh dong (rat pho bien vi tai khoan bot thuong cung la tai khoan ca nhan chu bot dung de tu quan ly group) - **TUYET DOI KHONG dung `event.isSelf` de loc ca event**, se bo sot dung truong hop nay (phat hien 2026-09-07 tu test that, xem chi tiet trong `src/adapters/zalo/CLAUDE.md`). Claim-once qua LedgerStore.tryClaimGroupJoinMessage() (bang group_join_messages RIENG voi welcome_messages, vi khac trigger). Day la 2 DM chao mung DOC LAP nhau (join group vs gui link dau tien) - cung 1 user co the nhan CA HAI, khong phai 1 cai thay the cai kia.
    session.ts                  luu/doc credentials (cookie/imei/userAgent) vao file JSON de khong phai quet QR lai moi lan restart
src/config/env.ts             doc + validate bien moi truong. Config Accesstrade theo merchant duoc suy ra tu MERCHANTS registry (ACCESSTRADE_CAMPAIGN_ID_<MERCHANT>, ACCESSTRADE_PROMOTIONS_MERCHANT_<MERCHANT>) — them merchant vao merchants.ts la du, khong can sua env.ts. Zalo Group Adapter mac dinh TAT (ZALO_GROUP_ENABLED=false).
src/core/settingsKeys.ts      (2026-08-21) hang so ten key cua 6 setting admin sua duoc qua /admin/settings (vd userSharePercent, withdrawalThresholdVnd, usageText, groupJoinWelcomeTemplate them 2026-09-07...) — file zero dependency, dung chung boi ca ledgerStore.ts (core) lan settingsRegistry.ts (config), tranh hardcode string key rai rac.
src/config/settingsRegistry.ts  (2026-08-21) mang SETTINGS_REGISTRY khai bao label/type/default/helpText/min/max cho tung setting — noi DUY NHAT can sua khi them 1 setting moi (route /admin/settings tu dong render form + validate theo registry nay, khong phai sua adminHtml.ts thu cong). Xem "Nguyen tac mo rong" trong docs/superpowers/specs/2026-08-21-admin-settings-menu-design.md.
src/index.ts                  wiring: tao LogStore/RateLimiter/Provider/Resolver mot lan, gan vao Express server + Telegram bot + Zalo bot (neu bat), graceful shutdown. notifyUser(platform,userId,message) (2026-08-20) dinh tuyen theo platform toi telegramBot.telegram.sendMessage hoac zaloBot.sendDirectMessage — ca 2 bien khai bao "let" TRUOC createServer() (giong pattern notifyAdmin voi telegramBot co san) de closure doc dung gia tri da gan sau khi ca 2 bot khoi dong xong, du ban than viec khoi dong zaloBot nam SAU loi goi createServer() trong code.
```

Luồng xử lý 1 request (dù vào từ HTTP hay Telegram) luôn đi qua `LinkResolverService.resolve()`:
`rate limit check → parseProductLink (validate + nhận diện merchant theo domain + resolve short-link redirect + extract shop_id/item_id nếu có pattern) → generate subId → provider.createAffiliateLink({merchant, ...}) → logStore.record() (kèm merchant) → trả ResolveLinkResult hoặc AppError`.

Mỗi request sinh một `subId` riêng (`{platform}-{userId}-{timestamp36}-{random}`) gắn vào affiliate link, dùng để đối soát hoa hồng sau này qua báo cáo của nguồn affiliate đang dùng — đây là lý do mọi log request đều lưu `subId`, và cũng là lý do `ledgerAdmin.ts` tra ngược `subId` → user qua `logStore.findBySubId()` khi ghi nhận đơn hàng thủ công.

### Nguyên tắc khi sửa code

- **Không phá vỡ ranh giới core/adapter**: `src/core/**` không được import bất cứ gì từ `express`, `telegraf`, hay biết về platform cụ thể. Logic nghiệp vụ mới luôn vào `src/core`, không vào adapter.
- **Adapter chỉ format I/O**: `src/adapters/*` và `src/api/server.ts` chỉ gọi `LinkResolverService`, bắt `AppError` để lấy `userMessage` hiển thị cho user — không tự implement lại validate/rate-limit/log.
- **Thêm merchant mới** (ví dụ Tiki): thêm 1 entry vào `MERCHANTS` trong `src/core/merchants.ts` (domain pattern + short-host nếu có) — `env.ts`, log, Telegram adapter đều tự động hỗ trợ merchant mới qua registry này, KHÔNG cần sửa. Nếu merchant có pattern URL chứa shop_id/item_id, thêm case trong `extractIds()` (`linkValidator.ts`) — mặc định trả `null` cho merchant chưa có pattern xác minh, **không đoán mò regex** (rủi ro sai âm thầm vì đây chỉ là metadata optional). **Ngoại lệ quan trọng — không phải merchant nào cũng chỉ cần đăng ký registry**: nếu network affiliate dùng endpoint/field HOÀN TOÀN KHÁC cho merchant đó (như TikTok Shop qua Accesstrade, xem ngay dưới), phải sửa thêm `AccesstradeProvider.createAffiliateLink` (branch theo `input.merchant`) và có thể cả `extractIds()`/`buildParsedLink()` nếu id tách được không còn là optional metadata mà là bắt buộc để gọi API — xem TikTok Shop làm ví dụ mẫu cho case này.
- **Sửa nội dung tin nhắn trả về link**: sửa `formatSuccessReply` trong `src/adapters/shared/replyText.ts` (dùng chung cho cả Telegram và Zalo, sửa 1 chỗ áp dụng cả 2). **2026-08-17: đã bỏ nhãn "Shopee 22%" cố định** (từng có trong `MERCHANT_LABELS`, xoá vì mâu thuẫn với hướng minh bạch mới — xem mục cashback bên dưới) — chỉ dùng tên thương hiệu thuần (`getMerchantConfig(merchant).displayName`). Nguyên tắc chung vẫn giữ: **không bịa % giảm giá/hoa hồng theo từng sản phẩm** trừ khi có nguồn dữ liệu xác thực (API chính thức, hoặc số cấu hình tĩnh chủ bot tự đặt và ghi rõ là ước tính).
- **Tin nhắn trả link hiện tại KHÔNG hứa hẹn số tiền/% hoa hồng cụ thể** (2026-08-17, tham khảo cách trình bày của 1 bot đối thủ rồi chỉnh lại chứ không copy nguyên) — chỉ nói rõ đơn cần thời gian để hệ thống affiliate xác nhận, hướng dẫn user nhắn `"xemhh"` để theo dõi. Đây là quyết định có chủ đích: hiển thị số ước tính đòi hỏi biết giá sản phẩm, mà tự động lấy giá từ trang Shopee vi phạm chính sách chống gian lận mục (e) (xem lịch sử trao đổi) — đừng tự ý thêm lại số ước tính (đ hay %) mà không nhắc lại rủi ro này với user trước.
- **Thêm nguồn affiliate mới** (khác Accesstrade): implement interface `AffiliateProvider` (`src/core/affiliateProvider.ts`), đăng ký trong `src/core/providers/index.ts` theo `env.affiliateProvider`.
- **Lỗi mới luôn là `AppError` subclass** (`src/core/errors.ts`) với `userMessage` tiếng Việt thân thiện — không throw lỗi thô lên adapter.
- **Adapter luôn log `err.message` trước khi trả `userMessage` cho user (2026-09-02)** — `zalo/bot.ts` và `telegram/bot.ts` trước đó chỉ lấy `err.userMessage` rồi vứt luôn `err.message` (chỗ chứa chi tiết chẩn đoán như `HTTP 400: {...}`), nên mọi lỗi tạo link đều không để lại dấu vết nào trong log Railway. Giờ cả 2 adapter `console.warn` kèm `code` + `rawUrl` + `message` trước khi reply. **Đừng bỏ dòng log này khi refactor** — không có nó thì mọi lỗi provider đều phải trace tay lại từ đầu.
- Biến môi trường mới thêm phải khai báo mặc định trong `src/config/env.ts` và note trong `.env.example`.

### Chi tiết đã tách ra khỏi file này (2026-09-02)

Nội dung dưới đây vẫn còn hiệu lực nguyên vẹn, chỉ chuyển sang nơi load theo nhu cầu để `CLAUDE.md` không quá dài:

- **Tích hợp affiliate theo từng merchant** (TikTok Shop qua Accesstrade, Shopee qua `an_redir`/`ShopeeAffiliateProvider`, Lazada, rút gọn link T3.2, các bug đã gặp + cách chẩn đoán lại) → skill `tich-hop-affiliate-provider` (`.claude/skills/tich-hop-affiliate-provider/SKILL.md`).
- **Đối soát hoa hồng tự động qua Accesstrade** (`accesstradeSync.ts`, mapping trạng thái, `updatePendingEntry`, `groupTransactionsByOrderId`, quy tắc "confirmed là cuối cùng", lịch chạy + timezone) → skill `doi-soat-hoa-hong` (`.claude/skills/doi-soat-hoa-hong/SKILL.md`).
- **Gotcha của `zca-js` / Zalo adapter** (override `paths` trong tsconfig, login QR/session, `uidFrom`/`isSelf`, pattern `.catch()`) → `src/adapters/zalo/CLAUDE.md` (tự load khi làm việc trong thư mục đó).
